# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# Runtime
ENVIRONMENT=development
LOG_LEVEL=INFO

# LLM providers (preference order: Groq -> OpenAI -> Anthropic, per Decision 4)
GROQ_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
